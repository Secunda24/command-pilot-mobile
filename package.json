{
  "name": "commandpilot-remote",
  "version": "1.0.0",
  "description": "CommandPilot Remote relay server — bridges phone browser to PC agent over the internet.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev":   "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws":      "^8.16.0"
  }
}
